# HTML Oracle Plan — Review Adjudication

Review of `a48399e` (`2026-09-22-html-oracle-harness.md`) by an independent
Luna xhigh reviewer returned **NO-GO**. This note records the primary agent's
decision on every finding. The plan must be revised to match these decisions
before implementation starts. Where a finding was caused by the plan
over-specifying machinery the user never asked for, the fix is to **remove**
that machinery, not to specify more of it.

## Governing requirement (from the user)

> Use the existing test cases, and have each one (as possible) create HTML or
> the appropriate "can't generate" errors using Sphinx and Sphinx-Needs
> (installed ../sphinx-needs). Get the harness set up, all the HTML files
> committed, and the ability to run Sphinx-Ultra against it and get a full set
> of failures.

Acceptance: (1) committed reference trees generated by real `sphinx-build -b
html`; (2) default `cargo test` stays green; (3) one opt-in command runs Ultra
over every runnable case and emits a complete, grouped failure report.

## Decisions

### Blocking

1. **Local sphinx-needs provenance — ACCEPT, simplified.** The `local_needs`
   uv lock pins sphinx-needs 8.5.0's *dependencies* (and Sphinx 9.1.0,
   Docutils 0.21.2). At run time the generator requires `--needs-root` (or
   `SPHINX_NEEDS_ROOT`), prepends `<root>/packages/sphinx-needs/src` to the
   child's `PYTHONPATH`, and before generating verifies in the child:
   `sphinx_needs.__file__` resolves inside that path, `sphinx_needs.__version__
   == "8.5.0"`, `git rev-parse HEAD` equals the pinned commit, `git rev-parse
   HEAD:packages/sphinx-needs` equals the pinned tree, and
   `git status --porcelain -- packages/sphinx-needs` is empty. Dirt outside
   `packages/sphinx-needs` (e.g. an untracked note at the repo root) is
   ignored. A dirty subtree is a hard error; no `--allow-dirty`. No relative
   `[tool.uv.sources]` path (worktrees live at different depths).
2. **Network denial in the child — ACCEPT.** Reference builds run through a
   small runner, `tools/html_oracle_runner.py`, which installs the socket
   guard (`socket.socket.connect`, `socket.create_connection`,
   `socket.getaddrinfo` raise) and then calls `sphinx.cmd.build.main(argv)`.
   The generator never runs Sphinx in-process. Test: a child-process build
   whose conf.py attempts a connection records the guard's error. No OS
   sandbox.
3. **Ledger schema completeness — ACCEPT.** The revised plan must show the
   full `index.json` schema (Python writer + Rust serde model) including:
   profile record (versions, lock sha256, needs commit/tree), per-case
   `status`, `exit_code`, normalized `warnings` text, `excluded_reason`,
   `origin` (source set, origin path, pytest node IDs for needs), `input_sha256`,
   `tree_sha256`, and `files`. Negative schema tests (unknown status, missing
   field, path escape, hash mismatch) come first. Normalization is **not**
   configurable in TOML (see H4); per-file policy is a fixed function of the
   logical path, written in both the generator and the Rust comparator.
4. **Default suite green — ACCEPT.** The default two-root test compares
   Ultra-to-Ultra only. Reference comparison lives solely in the ignored
   exhaustive test.
5. **Atomic generation — ACCEPT, simplified.** Each profile owns a subtree,
   `tests/fixtures/html_oracle/<profile>/`. Generation writes a complete new
   subtree into a sibling staging dir, validates it (every file referenced,
   every hash matches, no absolute root leaked), then swaps it in via rename
   (old → `.old`, staging → final, delete `.old`). Stale artifacts vanish
   because the whole subtree is replaced. One failure-injection test.

### High

1. **Needs case unit — DECIDE: one case per `doc_test` project.** The case is
   the project under `packages/sphinx-needs/tests/doc_test/<name>` built with
   `-b html` and its own `conf.py`. The ledger lists every pytest node ID that
   references that `srcdir` (static scan of `tests/**/*.py`), and sets
   `variants_not_captured = true` when any of those nodes passes
   `confoverrides` or a non-HTML builder. Per-node variants are explicit
   follow-up work, not this PR. We do **not** extract pytest expectations: the
   oracle is the real Sphinx + sphinx-needs build output. Drop the
   "reference-only because the directive is unsupported in Ultra" concept:
   needs cases are built, committed and run like any other; Ultra failing them
   is the known-red signal the user asked for.
2. **Needs root — ACCEPT.** Discovery is pinned to
   `packages/sphinx-needs/tests/doc_test/*/` (142 dirs with a `conf.py`) and
   rejects anything outside it.
3. **Profile verification tests — ACCEPT** for: wrong import location, wrong
   version, wrong commit, wrong tree, dirty subtree. Tampered-lock test is
   dropped; `uv run --locked` already enforces it.
4. **Generation determinism across roots — ACCEPT.** Test: generate one small
   source set into two different absolute staging roots and assert identical
   trees. The generator also fails if the absolute build root appears in any
   captured reference byte outside the warnings stream.
5. **Normalization APIs — ACCEPT, simplified.** Only two normalizations exist:
   CRLF→LF for text files, and replacement of the absolute source root with
   `<SRCDIR>` in the **warnings stream only**. Comparators: HTML/text files
   exact after CRLF; `searchindex.js` parsed (strip `Search.setIndex(`…`)`,
   compare JSON values, key order ignored, array order preserved);
   `objects.inv` header + decoded records; `.buildinfo` exact; everything else
   exact bytes. No `FieldBoundary`, no JSON pointers, no HTML DOM normalizer in
   this PR.
6. **Expected-failure and crash semantics — DECIDE: fold into status.**
   Statuses: `built` (reference exit 0), `build-error` (reference exits
   nonzero without a Python traceback; Ultra must match the exit class,
   warnings and whatever files were written; this is the "can't generate"
   path), `reference-crash` (traceback; not run), `excluded-network`,
   `excluded-plantuml`. Drop `alias`, `expectation`, `unsupported-builder`
   and `excluded-external-test-fixture`. Identical inputs simply produce
   identical blobs.
7. **Aggregate diagnostics — ACCEPT.** Sort by `(profile, source_set, case_id,
   logical_path, category)`. The exhaustive test writes a full report to
   `target/html-oracle/report.md` and `report.json` (per-case pass/fail plus
   mismatch categories, counts per source set and per category); stdout and
   the assertion message are capped at 64 KiB overall, with a pointer to the
   report.
8. **pytest in profiles — ACCEPT.** Add `pytest` to both profiles' locked deps;
   generator tests run via `uv run --locked --project tools/oracle_profiles/core
   python -m pytest tools/test_gen_html_oracle.py`.
9. **Offline lock generation — REJECT the original requirement.** Lock
   *creation* may use the network. Generation runs `uv run --locked` and the
   child network guard; that is the reproducibility contract.

### Medium / Low

- Timeout test: spawn the current test executable (`std::env::current_exe()`)
  re-entering an `#[ignore]`d helper test selected by an env var that sleeps or
  floods stdout. ACCEPT.
- `ExitStatusKind`: add `IoError(String)` for wait/read/kill failures; drain
  pipes to EOF after the cap. ACCEPT.
- Symlinks: reject during capture (check `is_symlink()` before `is_file()`).
  ACCEPT.
- Canonical hashes: `tree_sha256 = sha256("".join(f"{path}\0{sha256}\n" for
  sorted paths))`, same for `input_sha256`. ACCEPT.
- Inventory source set: the Sphinx-built projects in
  `tools/gen_inventory_fixture.py` become project cases; committed `.inv` files
  are not cases. ACCEPT.
- Licensing: drop per-file license records. `NOTICE.md` keeps one row per
  source set plus one row for Sphinx/alabaster theme static assets (BSD).
- Parser-only snippet eligibility: every docutils and Sphinx snippet becomes a
  one-document project (`index.rst` + the pinned conf matching
  `tools/gen_sphinx_fixture.py`: `extensions=[]`, `smartquotes=False`,
  `keep_warnings=True`). Whatever Sphinx does, success or error, is the oracle.
  **Pattern cases (881) are not documents and are out of scope for the HTML
  oracle**; state this in the plan and in `NOTICE.md`, and don't ledger them.
- Excluded statuses: classify statically: `excluded-plantuml` when conf.py
  loads `sphinxcontrib.plantuml`; `excluded-network` when conf.py references
  a remote URL that a build would fetch (a remote intersphinx *target* with a
  local inventory file, as in `tests/fixtures/intersphinx`, is fine).
- `HEAD~9` range and skill-name typo: fix.

### Additional constraints from the primary

- **Case directory names** must stay short for Windows paths:
  `<source_set>/<case_id>`, where `case_id` is sanitized to `[A-Za-z0-9_.-]`
  and truncated to 48 chars plus an 8-hex hash suffix on collision or
  truncation.
- **Storage**: HTML, `searchindex.js`, `objects.inv`, `.buildinfo`,
  `_sources/*` and warnings go under `refs/`; everything under `_static/` and
  `_images/` is content-addressed in `blobs/`. Report the total fixture size;
  flag if it exceeds 150 MB.
- **Parallelism**: the generator takes `-j N` (process pool, default
  `os.cpu_count()`); the exhaustive Rust test runs cases on a bounded thread
  pool (`available_parallelism`).
- **Filtering**: `HTML_ORACLE_FILTER=<substring>` restricts the exhaustive run
  to matching `profile/source_set/case_id` keys.
- The existing seven `tests/fixtures/*` HTML projects and the env/sphinx/
  docutils fixtures are **read only**; nothing in `src/` changes.

## Round 2 (re-review of f4a06d2) — decisions

1. **Local-needs determinism — ACCEPT.** `tools/html_oracle_runner.py`
   installs determinism shims before importing Sphinx, for every profile:
   `uuid.uuid4` is replaced by a per-process counter
   (`uuid.UUID(int=n, version=4)`, n = 1, 2, ...), and local-needs builds pass
   `-D needs_reproducible_json=1`. The shim list is recorded in each profile
   record (`determinism_shims: list[str]`). Test: build one local-needs project
   that emits both need HTML and `needs.json` twice under different roots and
   assert identical bytes. Ultra's IDs will not match the shimmed IDs until
   Ultra adopts the same scheme; that's an honest known-red mismatch, not
   something to hide.
2. **Warnings capture — ACCEPT.** The child runs Sphinx with `-q -w
   <warnings-file>`; the `warnings` field is that file only (normalized per the
   table). Combined stdout+stderr is kept only for traceback/status
   classification, and is not stored in `warnings`. Ultra is also run with
   `-q -w <file>`, and its warnings are compared from that file.
3. **`--verify` — ACCEPT.** `--verify --profile core` and `--verify --profile
   local_needs --needs-root ...` are separate commands, each run in its own
   locked environment. Task 9 runs both.
4. **Fresh run dirs — ACCEPT.** Delete and recreate each case's run directory
   before spawning Ultra; add a repeat-run test.
5. **PlantUML detection — ACCEPT.** AST detection covers `extensions` list
   entries, `import sphinxcontrib.plantuml`, and `app.setup_extension(...)`
   calls; `plantuml_from_app_extension` is the regression case.
6. **Reverse artifact validation — ACCEPT.** Validation fails on any file under
   `inputs/`, `refs/` or `blobs/` that the ledger doesn't reference, except
   `index.json`, `NOTICE.md` and per-case `warnings.txt`.

## Added scope (user request): `needs.json` oracle layer

For every local-needs case whose HTML reference status is `built` or
`build-error`, the generator performs a second reference build with `-b needs
-D needs_reproducible_json=1` into a separate output root and captures
`needs.json` as a second artifact on the same case, stored at
`refs/local_needs/sphinx_needs_doc_tests/<case_id>/needs/needs.json`. Schema:
add `needs_json: FileRecord | null` and `needs_status: CaseStatus | null`
(with `needs_exit_code`, `needs_warnings`) to `CaseRecord`, null for core
cases. Policy: parse JSON, compare values with object-key order ignored and
array order preserved. The exhaustive run invokes Ultra with `-b needs` for
these cases; if Ultra rejects the builder, that's a `needs-builder` category
mismatch, reported separately in `report.md`/`report.json` from HTML
mismatches. `needs.json` files that an HTML build writes (because the project
sets `needs_build_json = True`) stay in the HTML tree and get the same JSON
policy.

## Round 3 (user direction): diagnostics for a 1-for-1 reimplementation

User: "get the oracle projects / source and output needs.json and html being
easily comparable and a good diagnostic for building a 1-for-1 recoding of
this. The PR is just the oracle and diagnostics."

Scope: **no changes under `src/`**. The PR is the oracle corpus, generator,
comparator, runner and diagnostics. Comparison stays strict; everything below
changes what is *reported*, never what *passes*.

1. **Side-by-side trees on disk.** For every scheduled case, the exhaustive
   run leaves `target/html-oracle/runs/<profile>/<source_set>/<case_id>/` with
   `input/` (materialized source), `expected/` (reference tree reconstructed
   from refs + blobs, plus `warnings.txt` and, for local-needs,
   `needs/needs.json`), `actual/` (Ultra output), `actual-warnings.txt`,
   `actual-needs/` and `result.json`. A developer can run `diff -r expected
   actual` or open both in a diff tool with no harness knowledge. Each run dir
   is recreated fresh.
2. **Single-case command.** `HTML_ORACLE_FILTER=<key>` plus a documented
   `HTML_ORACLE_KEEP=all|failed` (default `failed`: run dirs of passing cases
   are deleted after comparison). The report prints, for each failing case,
   the exact filter string that reruns only that case.
3. **HTML mismatch localisation (report only).** For a text mismatch in an
   `*.html` file, split both sides at Sphinx's body markers (the
   `<div class="body" role="main">` element through its matching close, found
   by a simple tag-depth scan; no DOM library) and classify it as
   `html-body`, `html-chrome` (the body matches but the surrounding template
   differs), or `html-both`. Report the first differing line number and a
   unified diff (3 lines of context, capped at 64 KiB) of the body region
   first, then chrome. If the markers are missing on either side, fall back to
   a whole-file unified diff with category `html-unstructured`.
4. **needs.json diagnostics.** On a mismatch, key needs by
   `versions[*].needs[<id>]` and report `missing-need`, `extra-need`, and
   per-field `need-field` differences (need id, field, expected, actual), plus
   top-level/version key differences. Fall back to a JSON-path diff for
   unexpected shapes.
5. **searchindex.js / objects.inv diagnostics.** Report differing top-level
   keys (searchindex) and missing/extra/changed inventory records rather than
   one opaque "value differs".
6. **Warnings diagnostics.** Line-level diff of normalized warnings, with
   missing/extra counts.
7. **Report layout.** `report.md` has a summary table (per profile ×
   source_set: scheduled/passed/failed), a per-category count table, a
   "most common first-divergence" table (group failing HTML files by their
   first differing expected line, top 25, which shows the highest-leverage
   fixes), then per-case sections. `report.json` carries the same data in
   machine-readable form for tooling or agents.
8. **Default-suite diagnostics tests.** Unit tests for body/chrome splitting,
   needs.json keyed diff, inventory record diff and the divergence grouping,
   all against small synthetic inputs, so the default suite stays green.
